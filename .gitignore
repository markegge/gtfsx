# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
streamline_test.zip

# Wrangler local state
.wrangler/
.dev.vars
*.tsbuildinfo

# Stripe setup-script output (contains webhook signing secret)
.stripe-config-*.json

# D1 production backups (contain PII — emails, hashed credentials)
backups/

# Playwright MCP run artifacts (console logs, snapshots from test sessions)
.playwright-mcp/

# Playwright UI smoke artifacts
test-results/
playwright-report/

# Local task handoff / planning docs (not part of the repo)
handoffs/

# Archived/superseded docs — kept on disk for local reference, out of the repo.
# Living docs are docs/REQUIREMENTS.md + docs/ARCHITECTURE.md.
docs/archive/

# demand-dots build artifacts
demand-dots/cache/
demand-dots/.venv/
demand-dots/dots_*.geojson
tiles/*.ldjson
tiles/*.pmtiles
tiles/*.mbtiles
tiles/ldjson/
test/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
