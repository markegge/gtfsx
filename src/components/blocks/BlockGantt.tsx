/**
 * Center-pane vehicle-blocking Gantt (centerView === 'blocks').
 *
 * Placeholder shell — the full Remix-style vehicle-row × time-axis Gantt,
 * Quick Block heuristic, and cost header land in Part 2
 * (service-planning-2-blocking-and-runtimes).
 */
export function BlockGantt() {
  return (
    <div className="absolute inset-0 bg-white flex items-center justify-center text-warm-gray text-sm">
      Vehicle blocking — coming up.
    </div>
  );
}
